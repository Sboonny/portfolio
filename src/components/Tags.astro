---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<ul>
  {tags.map((tag)=> (
    <li class="mr-2 inline-block rounded-full bg-gray-200 px-3 py-1 text-sm font-semibold text-gray-700">
      {tag}
    </li>
  ))
  }
</ul>